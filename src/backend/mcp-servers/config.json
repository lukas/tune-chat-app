{
  "servers": [
    {
      "name": "filesystem",
      "description": "File system operations",
      "command": "npx",
      "args": ["@modelcontextprotocol/server-filesystem", "/Users"],
      "env": {
        "NODE_ENV": "production"
      }
    },
    {
      "name": "browsermcp",
      "description": "Browser automation for web browsing and interaction",
      "command": "npx",
      "args": ["@browsermcp/mcp@latest"],
      "env": {}
    }
  ]
}